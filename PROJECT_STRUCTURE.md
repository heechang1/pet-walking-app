# NFC Pet Walking App - 프로젝트 구조

## 📁 전체 디렉토리 구조

```
project/
├── app/                          # Next.js App Router 페이지
│   ├── page.tsx                  # 메인 페이지 (산책 시작/중/종료 버튼)
│   ├── start/
│   │   └── page.tsx              # 산책 시작 페이지 (프로필 + 시작 버튼)
│   ├── walking/
│   │   └── page.tsx              # 산책 중 페이지 (타이머 + 발바닥)
│   ├── end/
│   │   └── page.tsx              # 산책 종료 페이지 (총 산책 시간 요약)
│   ├── calendar/
│   │   └── page.tsx              # 달력 페이지 (도장 찍기)
│   ├── layout.tsx                # 루트 레이아웃
│   └── globals.css               # 전역 스타일
│
├── components/                    # 재사용 가능한 컴포넌트
│   ├── PrimaryButton.tsx         # 주요 버튼 (민트색)
│   ├── SecondaryButton.tsx        # 보조 버튼 (핑크/오렌지)
│   ├── ProfileCard.tsx           # 프로필 카드
│   ├── SummaryCard.tsx           # 요약 카드
│   └── TimerDisplay.tsx          # 타이머 표시
│
├── lib/                          # 유틸리티 함수
│   ├── time.ts                   # 시간 포맷팅 함수
│   └── push.ts                   # 푸시 알림 유틸리티
│
├── public/                       # 정적 리소스
│   ├── firebase-messaging-sw.js  # Firebase 서비스 워커
│   ├── stamp.png                 # 도장 이미지 (추가 필요)
│   ├── paw.png                   # 발바닥 아이콘 (추가 필요)
│   └── README.md                 # 리소스 안내
│
├── package.json                  # 프로젝트 의존성
├── tsconfig.json                 # TypeScript 설정
├── tailwind.config.js            # Tailwind CSS 설정
└── next.config.mjs               # Next.js 설정
```

## 🎨 디자인 시스템

### 컬러 팔레트 (파스텔 크림톤)
- **Primary (민트)**: `#A8DED0` - 주요 버튼, 강조 요소
- **Secondary Pink**: `#FBD3D3` - 보조 버튼, 카드 테두리
- **Secondary Orange**: `#F6C28B` - 보조 버튼, 산책 종료 버튼
- **Background**: `#FFFDF8` - 전체 배경색 (아이보리)
- **Text**: `#1f2937` (gray-900) - 주요 텍스트

### 컴포넌트 스타일
- **버튼**: `rounded-full`, `shadow-md`, `py-4 px-6`
- **카드**: `rounded-2xl`, `shadow-md`, `border`
- **애니메이션**: `animate-bounce`, `transition-all duration-200`

## 🔧 주요 기능

### 1. 타이머 기능
- ✅ 산책 페이지 진입 시 자동 시작
- ✅ localStorage에 타이머 상태 저장
- ✅ 새로고침해도 타이머 상태 유지
- ✅ HH:MM:SS 포맷 표시

### 2. 달력 및 도장 기능
- ✅ 월별 달력 UI
- ✅ 날짜 클릭 시 도장 데이터 저장 (localStorage)
- ✅ 산책 기록 표시
- ✅ 도장 이미지 placeholder 구조

### 3. 푸시 알림 구조
- ✅ Firebase 서비스 워커 파일 생성
- ✅ 알림 권한 요청 코드
- ✅ 푸시 설정 구조 (Firebase Key placeholder)

## 📝 데이터 저장 구조

### localStorage 키
- `walkingStartTime`: 산책 시작 시간 (ISO string)
- `walkingEndTime`: 산책 종료 시간 (ISO string)
- `walkingElapsed`: 경과 시간 (초)
- `walkingIsActive`: 산책 활성 상태 (boolean string)
- `walkRecords`: 산책 기록 배열 (JSON)
- `calendarStamps`: 달력 도장 데이터 (JSON)

## 🚀 실행 방법

```bash
# 개발 서버 시작
npm run dev

# 프로덕션 빌드
npm run build

# 프로덕션 서버 시작
npm run start
```

## 📦 필요한 리소스

다음 파일들을 `public/` 폴더에 추가해야 합니다:
- `stamp.png` - 도장 이미지 (64x64px 또는 128x128px)
- `paw.png` - 발바닥 아이콘 (192x192px)

자세한 내용은 `public/README.md`를 참고하세요.

## 🔐 Firebase 설정

푸시 알림을 사용하려면:
1. Firebase 프로젝트 생성
2. `lib/push.ts`의 `FIREBASE_CONFIG` 업데이트
3. `public/firebase-messaging-sw.js`의 설정 업데이트
4. Firebase SDK 설치: `npm install firebase`

## 📄 페이지 라우팅

- `/` - 메인 페이지
- `/start` - 산책 시작
- `/walking` - 산책 중
- `/end` - 산책 종료
- `/calendar` - 달력 및 도장


